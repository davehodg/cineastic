

DROP TABLE IF EXISTS movie;
CREATE TABLE movie (
       id          INT NOT NULL AUTO_INCREMENT,
       title       varchar(255),
       year        char(10),
       rating      char(4),
       PRIMARY KEY (id),
       KEY `title` (`title`),
       KEY rating (rating),
       unique      (title,year)
) ENGINE InnoDB CHARACTER SET utf8;

